apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.OfflineAgentName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "offline-agent.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
