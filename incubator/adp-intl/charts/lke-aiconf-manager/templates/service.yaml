apiVersion: v1
kind: Service
metadata:
  name: adp-aiconf-manager
  namespace: {{ .Release.Namespace }}
  labels:
    app: adp-aiconf-manager
spec:
  ports:
    - name: http
      port: 10001
      protocol: TCP
      targetPort: 10001
    - name: trpc
      port: 10002
      protocol: TCP
      targetPort: 10002
  selector:
    app: adp-aiconf-manager
  sessionAffinity: None
  type: ClusterIP
