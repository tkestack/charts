apiVersion: v1
kind: Service
metadata:
  labels:
    app: entity-extractor-server 
  name: entity-extractor-server
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: tcp-10001
      port: 10001
      protocol: TCP
      targetPort: 10001
  selector:
    app: entity-extractor-server 
  sessionAffinity: None
  type: ClusterIP
