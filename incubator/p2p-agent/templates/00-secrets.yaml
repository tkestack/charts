apiVersion: v1
kind: Secret
metadata:
  name: cos-secret
  namespace: {{ .Values.global.namespace }}
type: Opaque
data:
  # COS/MinIO credentials (base64 encoded)
  access-key-id: {{ .Values.seedServer.cosId | b64enc }}
  secret-access-key: {{ .Values.seedServer.cosKey | b64enc }}
