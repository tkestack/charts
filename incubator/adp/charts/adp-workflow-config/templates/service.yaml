apiVersion: v1
kind: Service
metadata:
  labels:
    app: adp-workflow-config
  name: adp-workflow-config
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: adp-workflow-config-8000
      port: 8000
      protocol: TCP
      targetPort: 8000
    - name: adp-workflow-config-8001
      port: 8001
      protocol: TCP
      targetPort: 8001
    - name: adp-workflow-config-8081
      port: 8081
      protocol: TCP
      targetPort: 8081
  selector:
    app: adp-workflow-config
  sessionAffinity: None
  type: ClusterIP