apiVersion: v1
kind: Service
metadata:
  name: {{ include "verl-on-tke.fullname" . }}
  labels:
    {{- include "verl-on-tke.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "verl-on-tke.selectorLabels" . | nindent 4 }}
